# Android wrapper scaffold (initial)

This folder is an initial Play Store readiness scaffold with fixed app identity and release version placeholders.

## Current status
- Package/application id set to `com.nomadspeak.app`.
- Version baseline set to `versionCode 1` / `versionName 1.0.0`.
- Portrait launch activity, launcher icon XML placeholders, and splash background are present.

## Next step
Replace this scaffold with a generated Capacitor Android project (`npx cap add android`) once npm package access is available in this environment.
